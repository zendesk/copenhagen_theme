<div class="container-divider"></div>
<div class="container">
  <nav class="sub-nav">
    <ol class="breadcrumbs">
       <li title="{{help_center.name}}">{{#link 'help_center'}}{{help_center.name}}{{/link}}</li>
       <li title="Contact Us">お問い合わせフォーム</li>
    </ol>
    {{!--breadcrumbs--}}
    {{search submit=false}}
  </nav>

  <h1>
    お問い合わせフォーム
    {{!--t 'submit_a_request'--}}
    {{#if follow_up}}
      <span class="follow-up-hint">
        {{follow_up}}
      </span>
    {{/if}}
  </h1>

  <div class="form">
    {{request_form}}
  </div>
</div>
